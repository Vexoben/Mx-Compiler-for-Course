package Middleend.llvmir.ValueAndUser;

import Middleend.llvmir.Type.IRBaseType;

public class toRegValue extends Value{

   public toRegValue(IRBaseType _type, String _name) {
      super(_type, _name);
   }
}
